public class liquidParticles extends Particle {
    public double density = 0;
    public double pressure = 0;

    public liquidParticles(double x, double y, double radius) {
        super(x, y, radius);
    }
}